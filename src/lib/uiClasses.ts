/**
 * Gemeinsame Klassen-Strings, die sonst vielfach hand-kopiert würden.
 * Eine Quelle statt Kopien – gleiche Optik, zentral anpassbar.
 */

/**
 * Eyebrow-Label im Mitgliederbereich (kleine, gesperrte Akzent-Zeile über
 * Überschriften). Bewusst die schmalere Sperrung `0.2em` der Panels – nicht die
 * `0.28em`+Haarlinie der Marketing-`<Eyebrow>`-Komponente, damit die Optik im
 * eingeloggten Bereich unverändert bleibt.
 */
export const memberEyebrow =
  "text-[0.7rem] font-semibold uppercase tracking-[0.2em] text-accent";
